# API Configuration
VITE_API_URL=http://localhost:8000

# Instructions:
# 1. Copy this file to .env in the frontend directory: copy .env.example .env
# 2. For production, set VITE_API_URL to your backend URL
# 3. For Docker deployment, this is handled automatically via docker-compose
